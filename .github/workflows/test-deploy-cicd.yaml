name: Test Deploy CI/CD

on:
  pull_request:

env:
  DOCKER_IMAGE: jcukier/discord-bot

permissions:
  contents: read
  packages: read

jobs:
  test-deploy:
    uses: ./.github/workflows/deploy-cicd.yaml
    with:
      docker_image: ${{ env.DOCKER_IMAGE }}
    secrets: inherit
